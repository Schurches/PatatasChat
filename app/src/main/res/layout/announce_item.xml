<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center"
    android:id="@+id/announce_layout"
    android:background="@drawable/announce_shape">

    <ImageView
        android:id="@+id/announce_image"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@drawable/oval_shape_alert"
        android:src="@drawable/ic_chat_alert"/>

    <TextView
        android:id="@+id/announce_message"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:textSize="18dp"
        android:textAlignment="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ImageView
        android:id="@+id/announce_image2"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@drawable/oval_shape_alert"
        android:src="@drawable/ic_chat_alert"/>


</LinearLayout>